services:
  postgres:
    image: postgres
    volumes:
      - ./mi-base-de-datos:/var/lib/postgresql/data
    container_name: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=myrt_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin123